    // if (
    //     Math.abs(pythonCount - rubyCount) >= 2 ||
    //     Math.abs(pythonCount - jsCount) >= 2 ||
    //     Math.abs(rubyCount - jsCount) >= 2
    // ) {
    //     return false
    // } else {
    //     return true
    // }