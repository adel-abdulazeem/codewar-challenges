
// make a constructor function with two methods (and only these two methods) to return and increment a counter, but the counter should not be directly accessible from outside the function.



function Counter() {

    let count =
        this.check = () => count
    this.increment = () => count++

};