// write a func tht takes name and return text or null
// name is str || empty || null
// str || nul
function greet(name) {
    // conditional
    if(name){
        return `hello ${name}!`
    }
    return null
}